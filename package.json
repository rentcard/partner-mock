{
  "name": "partner-mock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm:start --prefix frontend\" \"npm:start --prefix backend\"",
    "frontend": "cd ./frontend && npm install",
    "backend": "cd ./backend && npm install",
    "install": "npm run frontend && npm run backend"
  },
  "keywords": [],
  "author": "Martin Diessner",
  "license": "ISC",
  "devDependencies": { 
    "concurrently": "^8.2.2"
  }
}
